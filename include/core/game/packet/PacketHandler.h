#pragma once

#include "core/util/ByteBuffer.h"
#include <map>
#include <functional>
/*
template <typename M>
struct PacketDescription {
	std::function<void(M)> handle;
	std::funciton<M(ByteBuffer)>
};
class PacketHandler {
	std::map<int, PacketDescription> packetHandlers;
};*/